/* 子模块项目名，jar名 */
archivesBaseName = 'tcp-client'
jar.enabled = true
/* 需要org.springframework.boot插件 需要打包成可执行的jar*/
bootJar.enabled = true

dependencies {
    implementation project(':tcp-common') /* 子模块之间的依赖 */
}